// Header component
import React from "react";
import "./Header.css";

const Header = () => (
	<header className = "header">
		<h1>City Clicky Game!</h1>
		<h2>Test Your Memory.</h2>
		<h3>Click on an image to earn points, but don't click on any more than once!</h3>
	</header>
);


export default Header;